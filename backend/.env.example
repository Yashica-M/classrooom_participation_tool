# Server Configuration
PORT=8081
NODE_ENV=production

# Database Connection (MongoDB local or MongoDB Atlas connection string)
MONGODB_URI=mongodb://localhost:27017/syncpoll

# JWT Authentication Secret
JWT_SECRET=syncpoll_production_secret_key_2026

# Production Frontend CORS Origin (e.g., https://syncpoll.vercel.app or http://localhost:3001)
CORS_ORIGIN=http://localhost:3001
